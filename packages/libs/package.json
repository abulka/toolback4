{
  "name": "@toolback/libs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "The library shelf: dependencies listed here are pre-bundled to ESM and importable from user scripts as import('name') in previews and exports.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  }
}